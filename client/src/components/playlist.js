import React, { Component } from 'react';
import './playlist.css';

class Playlist extends Component {
    render() {
        return (
            <div className="playlist">
                This will show a single playlist
            </div>
        );
    }
}

export default Playlist;
