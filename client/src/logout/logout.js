import React, { Component } from 'react';
import './logout.css';

class Logout extends Component {
    render() {
        return (
            <div className="logout">
                This will be logout message
            </div>
        );
    }
}

export default Logout;
